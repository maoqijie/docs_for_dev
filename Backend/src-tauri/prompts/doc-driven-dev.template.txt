工作目录: {{WORKING_DIR}}
文档绝对路径: {{DOC_ABSOLUTE_PATH}}

【重要】你的任务是根据文档补全代码，而不是检查后停止。

## 执行步骤:

### 1. 理解需求
- 使用 Read 工具读取文档: {{DOC_ABSOLUTE_PATH}}
- 列出文档要求的所有功能点
- 明确每个功能需要的:
  * 接口/函数/方法
  * 数据结构/类型
  * 配置项
  * 数据库表和字段
  * 业务逻辑规则

### 2. 检查现状（必须逐项对照）
- 搜索现有相关代码
- 对照文档要求，逐项检查每个功能点:
  * ✅ 已完全实现
  * ⚠️ 部分实现（缺少具体什么）
  * ❌ 完全未实现

**关键**: 即使找到部分代码，也要继续检查所有功能点的完整性

### 3. 补全实现（直接修改代码）
对于每个 ⚠️ 或 ❌ 的功能:

a) 定位需要修改/创建的文件
b) 使用 Edit/Write 工具直接修改代码
c) 添加缺失的:
   - 函数实现
   - 数据验证
   - 错误处理
   - 日志记录
   - 配置项
   - 数据库操作

d) 每次修改后输出:
```
文件: 相对路径
行号: 大约行号
修改: 简要说明
```

### 4. 验证完整性
- 使用搜索工具确认修改已生效
- 对照文档的验收标准
- 确认所有功能点都已实现
- 列出还需要其他模块配合的功能（如果有）

### 5. 输出总结
```
## 实现完成报告

### 文档要求的功能点:
1. [功能名称] - [✅已存在 / ⚠️已补全 / ❌未实现]
2. [功能名称] - [✅已存在 / ⚠️已补全 / ❌未实现]
...

### 补全的文件:
- 文件路径: 修改内容说明
- 文件路径: 修改内容说明

### 还需要其他模块配合的功能（如果有）:
- [功能]: 需要 [模块] 实现 [接口]

{{COMPLETION_MARKER}}
```

## ⚠️ 禁止行为:
1. ❌ 找到现有代码就输出「已存在」然后停止
2. ❌ 只给分析报告，不修改文件
3. ❌ 说「需要手动修改」而不去修改
4. ❌ 创建测试文档代替实际代码实现

## ✅ 必须执行:
1. ✅ 必须读取整个文档，理解所有要求
2. ✅ 必须逐项检查现有代码的完整性
3. ✅ 必须直接修改代码，补全缺失的功能
4. ✅ 必须输出具体的文件路径和修改内容

现在开始执行，记住：不要只检查，要补全！
