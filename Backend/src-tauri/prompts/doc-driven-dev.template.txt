工作目录: {{WORKING_DIR}}

【重要】你的任务是根据文档补全代码，而不是检查后停止。

## 执行步骤:

### 1. 理解需求
- 读取文档: {{DOC_PATH}}
- 列出文档要求的所有功能点
- 明确每个功能的接口、数据结构、配置需求

### 2. 检查现状（必须逐项对照）
- 搜索现有相关代码
- 对照文档检查每个功能点:
  * ✅ 已完全实现
  * ⚠️ 部分实现（缺少X）
  * ❌ 完全未实现

**关键**: 即使找到代码也要继续检查完整性

### 3. 补全实现（直接修改代码）
对于每个 ⚠️ 或 ❌ 的功能:

a) 定位需要修改的文件
b) 使用工具直接修改代码
c) 添加缺失的功能

每次修改后输出:
```
文件: {{相对路径}}
行号: {{大约行号}}
修改: {{简要说明}}
```

### 4. 验证完整性
- 搜索确认修改已生效
- 对照文档验收标准
- 确认所有功能点已实现

### 5. 输出总结
```
## 实现完成报告

### 文档要求的功能点:
1. 功能1 - [✅已存在 / ⚠️已补全 / ❌未实现]
2. 功能2 - [✅已存在 / ⚠️已补全 / ❌未实现]
...

### 补全的文件:
- 文件1: 修改内容
- 文件2: 修改内容

{{COMPLETION_MARKER}}
```

## ⚠️ 禁止行为:
1. ❌ 找到代码就停止
2. ❌ 只给分析不修改
3. ❌ 说"需要手动修改"
4. ❌ 创建测试文档代替实现

## ✅ 必须执行:
1. ✅ 读取整个文档
2. ✅ 逐项检查完整性
3. ✅ 直接修改代码补全
4. ✅ 输出文件路径和变更

现在开始执行，记住：不要只检查，要补全！
